#include <stdio.h>

// 모든 벽을 칠하는데 걸리는 시간을 return

int solution(int walls) {
	int answer = 0;
	int painted_walls = 0;
	int i;
	for (i = 1; painted_walls < walls; i++) {
		painted_walls = (i)+__________ + __________________;
	}
	answer = _____________________;
	return answer;
}

int main() {
	int walls = 5;
	int ret = solution(walls);
	printf("%d \n", ret);
}