#include <stdio.h>
#include <stdlib.h>

// p63 매개변수가 배열
/*void solution(int p[]) {
	printf("p의 크기 %d\n", sizeof(p));
}

int main() {
	int arr[10] = { 0, };
	printf("arr의 크기 %d\n", sizeof(arr));
	solution(arr);

	return 0;
}
*/

// 함수 내 배열 선언 시 동적 할당으로 배열 선언
}